# Modelo Java — 3 clases

Solo `Course`, `CourseSection` y `ScheduledCourse`. Todas las propiedades de landing van aplanadas en `Course` (sin clases anidadas).

La API puede devolver `Course` + lista de `ScheduledCourse` por curso. El front mapea con `src/lib/course-mapper.ts`.
